# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Runtime
NODE_ENV="production"
PORT="3000"

# Security
SESSION_SECRET="replace_with_a_long_random_secret"

# Demo users (replace for production)
ADMIN_PASSWORD="replace_admin_password"
VIEWER_PASSWORD="replace_viewer_password"

# Optional: Hugging Face token for model inference API.
# Without token the app still works with heuristic fallback, but model quality may be limited.
HF_API_TOKEN=""
